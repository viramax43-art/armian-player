# Armian Player

Personal music player (Electron). Music folder defaults to the parent of `player/` (your `from_phone` tracks).

## Dev

```bash
npm install
npm run dev
```

## Build EXE

```bash
npm run pack
```

Portable EXE: `release/ArmianPlayer.exe`
